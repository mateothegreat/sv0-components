// src/routes/api/posts.remote.ts
// import { query } from "$app/server";

// export const getComponentCode = query(
//   async ({ params }: { params: { group: string; component: string } }) => {
//     const { group, component } = params;
//     const code = await fetch(
//       `https://api.github.com/repos/sv0/sv0/contents/components/${group}/${component}/demo.svelte`
//     );
//     const data = await code.json();
//     return data.content;
//   }
// );
